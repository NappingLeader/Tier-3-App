pipeline {
    agent {
        docker {
            image 'docker:24.0.6-dind'  // Docker-in-Docker image
            args '-u 0 -v /var/run/docker.sock:/var/run/docker.sock --dns=8.8.8.8'  // Mount Docker socket to allow Docker commands to run
        }
    }

    environment {
        DOCKER_IMAGE = 'docker:24.0.6-dind'   // Docker image to use
        DOCKER_CONFIG = "${env.WORKSPACE}/.docker"
        REGISTRY = 'docker754/cartservice'                 // Your Docker registry name
        DOCKER_CREDENTIALS_ID = 'docker-creds' // Jenkins credentials ID for Docker login
    }

    stages {
        stage('Checkout') {
            steps {
                git url: 'https://github.com/NappingLeader/Tier-3-App.git', branch: 'main'
            }
        }

        stage('Build & Test Services') {
            parallel {
                stage('cart') {
                    steps {
                        dir('three-tier-architecture-demo/cart') {
                            script {
                                // Build Docker image for the cart service
                                sh 'docker build -t $REGISTRY/cartservice:latest .'
                            }
                        }
                    }
                }
            }
        }

        stage('Push to Registry') {
            steps {
                withCredentials([usernamePassword(credentialsId: "$DOCKER_CREDENTIALS_ID", usernameVariable: 'USER', passwordVariable: 'PASS')]) {
                    script {
                        // Log in to the Docker registry
                        sh 'echo $PASS | docker login $REGISTRY -u $USER --password-stdin'
                        // Push the Docker image to the registry
                        sh 'docker push $REGISTRY/cartservice:latest'
                    }
                }
            }
        }
    }

    post {
        success {
            echo 'Microservices built and pushed to registry successfully!'
        }
        failure {
            echo 'Build or push failed. Please check the logs.'
        }
    }
}
